<%= form_with(model: parliamentary, local: true) do |form| %>
  <% if parliamentary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parliamentary.errors.count, "error") %> prohibited this parliamentary from being saved:</h2>

      <ul>
      <% parliamentary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :political_party %>
    <%= form.text_field :political_party %>
  </div>

  <div class="field">
    <%= form.label :birthdate %>
    <%= form.date_select :birthdate %>
  </div>

  <div class="field">
    <%= form.label :district_id %>
    <%= form.text_field :district_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
